Feature:
    operator:
            1. 一元运算:  ++, --, !, -     ++,--应前后缀
            2. 逻辑运算: ||, &&
            3. 位运算符: &, |, ^, ~, <<, >>
            4. 算术运算: +, -, *, /, %
            5.关系运算: >, >=, <, <=, ==, !=
            6. 复合赋值运算: +=, -=, *=, /=, %=
     控制流: 
　　　for loop, while loop, do-while loop
    判断语句: if, else
    数据类型: bool, char, int, float, 一维数组

1. 词法分析器

